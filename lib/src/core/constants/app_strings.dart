/// Hardcoded strings used in non-localized contexts (logs, keys, etc.)
class AppStrings {
  AppStrings._();

  // App
  static const String appTitle = 'Countries Explorer';

  // Storage keys
  static const String keyThemeMode = 'theme_mode';
  static const String keyLocale = 'locale';
}
